{
  "name": "tomkat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "tomkat is a backend management system developed using the Tauri 2.x framework.",
  "author": {
    "name": "photowey",
    "email": "photowey@gmail.com",
    "url": "https://github.com/photowey"
  },
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "serve": "vite",
    "build:dev": "vue-tsc && vite build --mode dev",
    "build:test": "vue-tsc && vite build --mode test",
    "build:stage": "vue-tsc && vite build --mode stage",
    "build:prod": "vue-tsc && vite build --mode prod",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "typescript": "^5.2.2",
    "vite": "^5.3.1",
    "vue-tsc": "^2.0.22",
    "@tauri-apps/cli": "^2"
  }
}
